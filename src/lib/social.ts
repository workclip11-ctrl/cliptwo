import type { Platform } from "./types";

// Platforms a clipper can connect. Extend this list to add more (e.g. Reels).
// The Social Accounts UI renders one row per supported platform.
export const SUPPORTED_PLATFORMS: Platform[] = ["Instagram", "YouTube"];
